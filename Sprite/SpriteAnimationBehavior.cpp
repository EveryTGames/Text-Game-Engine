#include "SpriteAnimationBehavior.h"

// Currently implementation is header-only for simplicity. If later the user
// wants to move methods to the .cpp, we can split items out.

// Keep this file so build system still picks up the unit and to allow future
// expansion.
